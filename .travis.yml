#Travis-ci config file

sudo: required
dist: trusty

services:
  - docker

before_install:
  - docker build -t patrick .

script:
  - docker run -v `pwd`:/tmp/build --workdir /tmp/build/map_gallery --entry-point make patrick -B

notifications:
  email:
    recipients:
      - gliese.q@gmail.com
      - kotaimen.c@gmail.com
